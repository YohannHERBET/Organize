import './errorLogin.scss';

const ErrorLogin = () => (
  <div className="error-page">
    <h1 className="title-error">404</h1>
    <p className="text-error">Email et/ou mot de passe incorrect(s)</p>
  </div>
);

export default ErrorLogin;
